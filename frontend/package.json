{
  "name": "recoverai-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173"
  },
  "//": "vite + plugin-react are intentionally in dependencies (not devDependencies) so a clean `npm install` always installs them even when NODE_ENV=production or --omit=dev is set. Without this, `npm run dev` fails with 'vite is not recognized'.",
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^5.4.10",
    "@vitejs/plugin-react": "^4.3.3"
  }
}
